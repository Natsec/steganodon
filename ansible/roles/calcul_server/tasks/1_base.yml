- name: Timezone
  shell: timedatectl set-timezone Europe/Paris

- name: Installation des paquets
  package:
    name: "{{ item }}"
    state: latest
  loop:
    - tmux
    - htop
    - ncdu
    - tree
